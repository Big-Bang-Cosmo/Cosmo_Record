
<h1><%= @user.last_name+ @user.first_name %>様のページ</h1>

<div class="sidebar">
	<div class="genre-item">
		<p>ジャンル</p>
		<ul>
			<% @genres.each do |genre| %>
			<li><%= genre.genre_name %></li>
			<% end %>
		</ul>
	</div>
</div>

<div class="ranking">
	<div class="ranking-item">
		<p>売上ランキング</p>
		<ol>
			<li></li>
			<li></li>
			<li></li>
			<li></li>
			<li></li>
		</ol>
	</div>
</div>

<h4>お気に入り商品</h4>

<% @items.each do |item| %>
	<table border="1">
			<tr>
				<td><%= attachment_image_tag item, :item_image, :fill, 60, 60, fallback: "no_image.jpg" %></td>
				<td><%= item.item_name %></td>
				<td><%= item.artist.artist_name %></td>
				<td><p>価格（税込）：</p><%= item.price * 1.1 %></td>
                <td>
					<% if item.favorited_by?(current_user) %>
					    <%= link_to user_item_favorites_path(item), method: :delete do %>
					      <span class="glyphicon glyphicon-heart" aria-hidden="true" style="color: red;"></span>
					      <%= item.favorites.count %>
					    <% end %>
					<% else %>
					    <%= link_to user_item_favorites_path(item), method: :post do %>
					      <span class="glyphicon glyphicon-heart-empty" aria-hidden="true"></span>
					      <%= item.favorites.count %>
					    <% end %>
					<% end %>
                </td>
			</tr>
	</table>
<% end %>

<%= paginate @items, class: "paginate" %>


