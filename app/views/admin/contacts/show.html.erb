<h3>お問い合わせ詳細</h3>
<div class="top-link"><%= link_to 'お問い合わせ一覧へ戻る', admin_contacts_path%></div>
<div contact_info>
	<div class="row  contact_user_info clearfix">
		<div class="col-lg-2">
			<%= l @contact.created_at, format: :short %>
		</div>
		<div class="col-lg-2">
			<p>名前: <%= @contact.last_name + @contact.first_name %></p>
		</div>
		<div class="col-lg-5">
			<p>メールアドレス: <%= @contact.email %></p>
		</div>
		<div class="col-lg-3">
			<p>電話番号: <%= @contact.phone_number %></p>
		</div>
	</div>

	<div class="contact_body">
		<%= @contact.contact_body %>
	</div>
</div>






  <% if @contact.status == "未対応" %>
	<%= button_to "未対応", {controller: 'admin/contacts', action: 'update'}, {method: :patch} %>
  <% else %>
	<%= button_to "応対完了", {controller: 'admin/contacts', action: 'update'}, {method: :patch} %>
  <% end %>