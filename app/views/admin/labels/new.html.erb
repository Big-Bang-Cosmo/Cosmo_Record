<h4>レーベル名登録</h4>

<div class="new">
<%= form_for(@label, url: admin_labels_path	) do |f|%>
	 	<%= f.text_field :label_name %>
	 <%= f.submit "登録する"%>
<% end %>
</div>

<p><%= link_to '戻る', admin_home_top_path%></p>