<h4>ジャンル名登録</h4>

<div class="new">
<%= form_for(@genre, url: admin_genres_path) do |f|%>
		<%= f.text_field :genre_name %>
	<%= f.submit "登録する"%>
<% end %>
</div>

<p><%= link_to '戻る', admin_home_top_path%></p>